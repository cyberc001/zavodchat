openapi: 3.0.0
info:
  title: Zavodchat (REST)
  version: 1.0.0
paths:
  /auth:
    post:
      summary: Вход в систему по логину/паролю
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string 
                  example: chetkii_poc
                password:
                  type: string
                  example: 12345678
      responses:
        '200':
          description: Успешная авторизация
          headers:
            Set-Cookie:
              schema:
                type: string
                example: zavodchat_token=3c3beb77-b54a-4733-8c5d-acd7b2680241
          content:
            text:
              example: 3c3beb77-b54a-4733-8c5d-acd7b2680241
        '404':
          description: Неверный логин/пароль
  /register:
    post:
      summary: Регистрация в системе
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string
                  example: chetkii_poc
                displayname:
                  type: string
                  example: Чёткий П0цык
                password:
                  type: string
                  example: 12345678
      responses:
        '200':
          description: Успешная регистрация
        '400':
          description: Слишком короткий username/password
        '403':
          description: Логин занят
    put:
      summary: Изменение данных пользователя
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                username:
                  type: string
                  example: realnii_poc
                displayname:
                  type: string
                  example: рЯльНыЙ____ПОЦЦЦЦЦ
                password:
                  type: string
                  example: letmein123
                avatar:
                  type: string
                  format: binary
                  example: (Любой поддерживаемый формат изображений)
                  

      responses:
        '200':
          description: Успешное изменение данных
  /users/$user_id:
    get:
      summary: Информация о пользователе
      responses:
        '200':
          description: "Информация о пользователе"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: Не существует пользователя с таким user_id
  /user_status:
    put:
      summary: Изменить свой статус
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: status
          in: query
          required: true
          description: Статус
          schema:
            $ref: '#/components/schemas/UserStatus'
      responses:
        '404':
          description: Не существует пользователя с таким user_id

  /friends:
    get:
      summary: Список друзей
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: "Список друзей"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
  /friend_requests:
    get:
      summary: Список запросов дружбы
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: "Список пользователей, от которых есть исходящий запрос дружбы"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
  /friends/$user_id:
    post:
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      summary: Отправить или принять запрос дружбы
      responses:
        '200':
          description: "Запрос дружбы был отправлен или принят"
        '202':
          description: "Пользователь уже состоит в друзьях / запрос дружбы уже отправлен"
        '400':
          description: "Нельзя отправить запрос дружбы себе"
        '403':
          description: "Пользователь заблокировал вас"
    delete:
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      summary: Удалить из друзей или отменить/отклонить запрос дружбы
      responses:
        '200':
          description: "Пользователь был удалён из друзей / запрос дружбы был отменён (отклонён)"
        '403':
          description: "Пользователь не состоит в друзьях"
  
  /blocked_users:
    get:
      summary: Список заблокированных пользователей
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: "Список заблокированных пользователей"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
  /blocked_users/$user_id:
    post:
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      summary: Заблокировать пользователя
      description: Также удаляет пользователя из друзей.
      responses:
        '200':
          description: "Пользователь был заблокирован"
        '202':
          description: "Пользователь уже заблокирован"
        '400':
          description: "Нельзя заблокировать себя"
    delete:
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      summary: Разблокировать пользователя
      responses:
        '200':
          description: "Пользователь разблокирован"
        '202':
          description: "Пользователь не был заблокирован"

  /servers:
    get:
      summary: Список серверов, в которых состоит пользователь
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Список серверов
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Server'
    post:
      summary: Создать сервер
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                name:
                  type: string
                  example: КЛУБ ВЕСЁЛЫХ И НАХОДЧИВЫХ
                avatar:
                  type: string
                  format: binary
                  example: (Любой поддерживаемый формат изображений)
      responses:
        '200':
          description: server_id созданного сервера
          content:
            text:
              example: 73
        '403':
          description: Превышено ограничение на количество серверов, которыми владеет пользователь
  /servers/$server_id:
    get:
      summary: Получить информацию о сервере
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: "Информация о сервере"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Server'
    put:
      summary: Изменить параметры сервера
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                name:
                  type: string
                  example: настоящие поцыки
                owner_id:
                  type: int
                  example: 401
                avatar:
                  type: string
                  format: binary
                  example: (Любой поддерживаемый формат изображений)
      responses:
        '200':
          description: Сервер успешно изменён
        '202':
          description: Пользователь с owner_id уже является владельцем сервера
        '400':
          description: Слишком длинное название сервера
        '404':
          description: Пользователь с owner_id не состоит на сервере
    delete:
      summary: Удалить сервер
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Сервер успешно удалён
  /servers/$server_id/users:
    get:
      summary: Пагинированный список пользователей
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: start_id
          required: true
          in: query
          description: id, с которого начинается список. Чтобы получить список с начала для order=1, start_id=2147483647
          schema:
            type: integer
            example: 0
        - name: count
          required: true
          in: query
          description: Размер диапазона
          schema:
            type: integer
            example: 50
        - name: order
          in: query
          description: Порядок id пользователей (0 - по возрастанию, 1 - по убыванию [по умолчанию])
          schema:
            type: integer
            example: 0
        - name: displayname
          in: query
          description: Фильтр по частичному имени пользователя
          schema:
            type: string
            example: Vasya
      responses:
        '200':
          description: Список пользователей (может быть < count - означает конец массива пользователей)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServerUser'
        '403':
          description: Неправильный start (< 0, >= количеству пользователей) / слишком большой count
  /servers/$server_id/users/$user_id:
    get:
      summary: Информация о пользователе на сервере
      responses:
        '200':
          description: Информация о пользователе
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerUser'
    delete:
      summary: Выгнать пользователя с сервера
      responses:
        '200':
          description: Пользователь был кикнут
        '403':
          description: Нет прав на кик / попытка кикнуть пользователя >= ролью / попытка кикнуть владельца
        '404':
          description: Пользователь не состоит на сервере
  /servers/$server_id/channels:
    get:
      summary: Список каналов
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Информация обо всех каналах
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Channel'
    post:
      summary: Создать канал
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                name:
                  type: string
                  example: тексты весёлых лет
                type:
                  type: integer
                  example: 0
      responses:
        '200':
          description: channel_id созданного канала
          content:
            text:
              example: 4
        '403':
          description: Превышено ограничение на количество каналов на сервере
  /channels/$channel_id:
    get:
      summary: Информация о канале
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Информация о канале
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
    put:
      summary: Изменить параметры канала
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                name:
                  type: string
                  example: радио судного дня
                type:
                  type: integer
                  example: 1
      responses:
        '200':
          description: Параметры успешно изменены
    delete:
      summary: Удалить канал
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Канал успешно удалён
  /channels/$channel_id/messages:
    get:
      summary: Пагинированный список сообщений
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: start_id
          required: true
          in: query
          description: id, с которого начинается список. Чтобы получить список с начала для order=1, start_id=2147483647
          schema:
            type: integer
            example: 0
        - name: count
          required: true
          in: query
          description: Размер диапазона
          schema:
            type: integer
            example: 50
        - name: order
          in: query
          description: Порядок id сообщений (0 - по возрастанию, 1 - по убыванию [по умолчанию])
          schema:
            type: integer
            example: 0
      responses:
        '200':
          description: Сообщения (может быть < count - означает конец массива сообщений)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Message'
        '403':
          description: Неправильный start (< 0, >= количеству пользователей) / слишком большой count / канал не текстовый
    post:
      summary: Отправить сообщение
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                text:
                  type: string
                  description: Текст сообщения
                  example: Ушёл за сигаретами
                attachments:
                  type: array
                  description: приложения к сообщению
                  items:
                      $ref: '#/components/schemas/Attachment'
      responses:
        '400':
          description: Пустое сообщение
        '200':
          description: ID созданного сообщения
          content:
            text:
              example: 377
  /channels/$channel_id/messages_search:
    post:
      summary: Пагинированный фильтрованый список сообщений
      description: Указанные в теле запроса ограничения находятся в коньюнкции (И), включая массив "content". "content" также может быть строкой.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: array
                  example:
                    - "pc"
                    - "gaming"
                  description: Искомая подстрока
                author_id:
                  type: integer
                  example: 444
                  description: ID пользователя, отправившего сообщение
                date_from:
                  type: string
                  example: "2024-11-13 00:00:00+03"
                  description: дата, не раньше которой отправлено сообщение
                date_until:
                  type: string
                  example: "2024-11-13 00:00:00+03"
                  description: дата, не позже которой отправлено сообщение
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: start_id
          required: true
          in: query
          description: id, с которого начинается список. Чтобы получить список с начала для order=1, start_id=2147483647
          schema:
            type: integer
            example: 0
        - name: count
          required: true
          in: query
          description: Размер диапазона
          schema:
            type: integer
            example: 50
        - name: order
          in: query
          description: Порядок id сообщений (0 - по возрастанию, 1 - по убыванию [по умолчанию])
          schema:
            type: integer
            example: 0
      responses:
        '200':
          description: Сообщения (может быть < count - означает конец массива сообщений)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Message'
        '403':
          description: Неправильный start (< 0, >= количеству пользователей) / слишком большой count / канал не текстовый
  /messages/$message_id:
    get:
      summary: Информация о сообщении
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Сообщение
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Message'
    put:
      summary: Отредактировать сообщение
      description: Поля text и attachments можно опустить - в таком случае они не будут изменены. Attachments полностью заменяются на указанные.
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                text:
                  type: string
                  description: Текст сообщения
                  example: Вернулся с молоком
                attachments:
                  type: array
                  description: приложения к сообщению
                  items:
                      $ref: '#/components/schemas/Attachment'
      responses:
        '400':
          description: Пустое сообщение
        '403':
          description: Пользователь не является автором сообщения
        '200':
          description: Сообщение успешно отредактировано
    delete:
      summary: Удалить сообщение
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '403':
          description: Пользователь не является автором сообщения и не имеет право на удаление чужих сообщений
        '200':
          description: Сообщение успешно удалено
  /server_invites/$invite_id:
    get:
      summary: Получить информацию о сервере приглашения
      responses:
        '200':
          description: "Информация о сервере"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Server'
    post:
      summary: Вступить на сервер по приглашению
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Пользователь был принят на сервер
        '202':
          description: Пользователь уже состоит на сервере
        '404':
          description: Приглашение истекло / пользователь забанен
  /servers/$server_id/invites:
    get:
      summary: Получить список приглашений на сервер
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Информация о приглашениях
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Invite'
        '403':
          description: У пользователя нет прав на управление приглашениями
    post:
      summary: Создать приглашение
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: expires
          in: query
          description: Дата и время истечения приглашения (автоматически удаляется из базы данных через некоторый промежуток). Может быть "never"
          schema:
            type: string
            format: date
            example: "2024-11-13 12:50:15+03"
      responses:
        '200':
          description: ID созданного приглашения
          content:
            text:
              example: 1e1d184c-679a-494e-ba30-6f1f72bd221e
        '400':
          description: Неправильный формат даты и времени expires
        '403':
          description: Превышено ограничение на количество приглашений / у пользователя нет прав на управление приглашениями
  /servers/$server_id/invites/$invite_id:
    get:
      summary: Информация о приглашении на сервер
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Информация о приглашении
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invite'
        '403':
          description: У пользователя нет прав на управление приглашениями
        '404':
          description: Приглашения с таким ID не существует на сервере
    put:
      summary: Изменить параметры приглашения
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: expires
          in: query
          description: Дата и время истечения приглашения (автоматически удаляется из базы данных через некоторый промежуток). Может быть "never"
          schema:
            type: string
            format: date
            example: "2024-11-13 12:50:15+03"
      responses:
        '200':
          description: Приглашение успешно изменено
        '400':
          description: Неправильный формат даты и времени expires
        '403':
          description: У пользователя нет прав на управление приглашениями
        '404':
          description: Приглашения с таким ID не существует на сервере
    delete:
      summary: Удалить приглашение
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Приглашение успешно удалено
        '403':
          description: У пользователя нет прав на управление приглашениями
        '404':
          description: Приглашения с таким ID не существует на сервере
  /servers/$server_id/bans/:
    get:
      summary: Пагинированный список банов
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: start_id
          required: true
          in: query
          description: id, с которого начинается список. Чтобы получить список с начала для order=1, start_id=2147483647
          schema:
            type: integer
            example: 0
        - name: count
          required: true
          in: query
          description: Размер диапазона
          schema:
            type: integer
            example: 50
        - name: order
          in: query
          description: Порядок id бановв (0 - по возрастанию, 1 - по убыванию [по умолчанию])
          schema:
            type: integer
            example: 0
      responses:
        '200':
          description: Список забаненных пользователей с полем expires (может быть < count - означает конец массива банов)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserBan'
        '403':
          description: Неправильный start (< 0, >= количеству банов) / слишком большой count
  /servers/$server_id/bans/$user_id:
    post:
      summary: Забанить пользователя
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: expires
          in: query
          description: Дата и время истечения бана (автоматически удаляется из базы данных через некоторый промежуток). Может быть "never" - бан навсегда
          schema:
            type: string
            format: date
            example: "2024-11-13 12:50:15+03"
      responses:
        '200':
          description: Пользователь успешно забанен
        '202':
          description: Пользователь уже забанен
        '400':
          description: Неправильная дата и время expires
        '403':
          description: Нет прав на управление банами / попытка забанить >= роль
  /servers/$server_id/bans/$ban_id:
    put:
      summary: Изменить параметры бана
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: expires
          in: query
          description: Дата и время истечения бана (автоматически удаляется из базы данных через некоторый промежуток). Может быть "never" - бан навсегда
          schema:
            type: string
            format: date
            example: "2024-11-13 12:50:15+03"
      responses:
        '200':
          description: Бан успешно изменён
        '400':
          description: Неправильная дата и время expires
        '404':
          description: Пользовавтель не забанен
    delete:
      summary: Разбанить пользователя
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Пользователь успешно разбанен
        '403':
          description: Нет прав на управление банами
        '404':
          description: Пользователь не забанен
  /servers/$server_id/roles:
    get:
      summary: Список ролей
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Список ролей в порядке от самой старшей до самой младшей
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Role'
    put:
      summary: Изменить список ролей
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/Role'
      responses:
        '200':
          description: ID созданной роли
          content:
            text:
              example: 17
        '400':
          description: Неправильные параметры / попытка вставить роль перед default ролью
  /servers/$server_id/users/$user_id/roles/$role_id:
    post:
      summary: Назначить пользователю роль
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Роль успешно назначена
        '202':
          description: Пользователь уже имеет эту роль
    delete:
      summary: Отнять у пользователя роль
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Роль успешно отобрана
        '400':
          description: Попытка отобрать роль по умолчанию
  /files/avatar/user/$filename:
    get:
      summary: Аватар пользователя
      responses:
        '200':
          description: Файл с аватаром
          content:
            file:
              example: (Любой файл поддерживаемого формата изображений)
        '404':
          description: Файла не существует
  /files/avatar/server/$filename:
    get:
      summary: Аватар сервера
      responses:
        '200':
          description: Файл с аватаром
          content:
            file:
              example: (Любой файл поддерживаемого формата изображений)
        '404':
          description: Файла не существует
  /files/upload/:
    post:
      summary: Загрузить файл на сервер
      parameters:
        - $ref: "#/parameters/zavodchat_token"
        - name: ext
          required: true
          in: query
          description: Расширение файла
          schema:
            type: string
            example: png
        - name: file
          in: query
          description: Файл
          schema:
            type: string
            format: binary
      responses:
        '400':
          description: Пустое расширение / слишком длинное расширение / пустой файл / файл не помещается на отведённое пользователю место на диске
        '200':
          description: Случайно сгенерированное имя файла
          content:
            text:
              example: "yN2ZIvEAa5YBT05qGVaBlG1mxDCY7n1v.png"
  /files/upload/$filename:
    delete:
      summary: Удалить свой файл с сервера
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '404':
          description: Файла не существует
        '200':
          description: Файл был удалён
  /files/upload/$user_id/$filename:
    get:
      summary: Файл с сервера
      parameters:
        - $ref: "#/parameters/zavodchat_token"
      responses:
        '200':
          description: Файл
          content:
            file:
              example: (Содержимое файла)
        '404':
          description: Файла не существует
  /params:
    get:
      summary: Конфигурация сервера
      responses:
        '200':
          description: Информация о конфигурации сервера
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Config'

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
          example: 412
        name:
          type: string
          example: cyb3rc001
        avatar:
          type: string
          example: avatar_453912.png
        status:
          type: integer
          example: 0
    UserBan:
      type: object
      properties:
        id:
          type: integer
          example: 33
        expires:
          type: string
          format: date
          example: "2026-01-08T05:05:48.638Z"
        user:
          $ref: '#/components/schemas/User'
    ServerUser:
      type: object
      properties:
        id:
          type: integer
          example: 412
        name:
          type: string
          example: cyb3rc001
        avatar:
          type: string
          example: avatar_453912.png
        status:
          type: integer
          example: 0
        roles:
          description: role_id ролей пользователя на сервере
          type: array
          items:
            type: integer
          example:
          - 3
          - 7
    VCState:
      type: object
      properties:
        id:
          type: integer
          example: 412
        mute:
          type: integer
          example: 1
        deaf:
          type: integer
          example: 0
    Server:
      type: object
      properties:
        name:
          type: string
          example: Зона
        avatar:
          type: string
          example: avatar_453912.png
    Channel:
      type: object
      properties:
        id:
          type: integer
          example: 2
        name:
          type: string
          example: channel_test_vc
        type:
          description: 0 - текстовый, 1 - голосовой
          type: integer
          example: 1
        vc_users:
          description: user_id пользователей, которые подключены к голосовому каналу
          type: array
          items:
              $ref: '#/components/schemas/VCState'
    Attachment:
      type: object
      properties:
        type:
          type: integer
          description: 0 - ссылка, 1 - файл, 2 - изображение
          example: 2
        content:
          type: string
          description: URL к файлу либо ссылке
          example: "/files/upload/1/94fkwekl1u93w.png"
    Message:
      type: object
      properties:
        id:
          type: integer
          example: 17
        author_id:
          type: integer
          example: 3
        sent:
          type: string
          format: date
          example: "2024-11-09 22:25:28.021376+03"
        edited:
          type: string
          format: date
          example: "2024-11-09 22:25:28.021376+03"
        text:
          type: string
          example: "папа ты купил молоко?"
        attachments:
          description: приложения к сообщению
          type: array
          items:
              $ref: '#/components/schemas/Attachment'
    Invite:
      type: object
      properties:
        id:
          type: string
          example: 1e1d184c-679a-494e-ba30-6f1f72bd221e
        expires:
          type: string
          format: date
          example: "2024-11-13 00:00:00+03 или never"
        server_id:
          type: integer
          example: 3
    Role:
      type: object
      properties:
        id:
          type: integer
          example: 2
        color:
          type: string
          example: "#0EDDDD"
        name:
          type: string
          example: "admin"
        perms1:
          type: integer
          example: 158377
    
    Config:
      type: object
      properties:
        max_channels_per_server:
          type: integer
          description: Максимальное количество каналов, которые можно создать на одном сервере
          example: 50
        max_get_count:
          type: integer
          description: Максимальное количество записей, которые можно получить за один запрос пагинации
          example: 50
        max_attachments:
          type: integer
          description: Максимальное количество приложений к сообщению
          example: 10
        max_roles_per_server:
          type: integer
          description: Максимальное количество ролей, которые можно создать на одном сервере
          example: 50
        max_video_bitrate:
          type: integer
          description: Максимальный битрейт видео, который можно запросить при его включении в голосовом канале
          example: 10240000
        min_password_length:
          type: integer
          description: Минимальная длина пароля при регистрации / изменении пароля
          example: 8
        min_username_length:
          type: integer
          description: Минимальная длина имени пользователя при регистрации / изменении пароля
          example: 2
        max_servers_owned_per_user:
          type: integer
          description: Максимальное количество серверов, которыми может владеть один пользователь
          example: 10
        total_file_storage_size:
          type: integer
          description: Место на диске, выделенное под хранение файлов пользователей. Пропроционально разделено между всеми пользователями.
          example: 10737418240
        ws_port:
          type: integer
          description: Порт для сообщений WebSocket
          example: 444
        ws_vc_port:
          type: integer
          description: Порт для сообщений WebSocket голосового канала
          example: 445
          
    UserStatus:
      type: integer
      enum:
        - 0
        - 1
        - 2
        - 3
      x-enum-descriptions:
        - Не в сети / невидимый
        - В сети
        - Отошёл
        - Не беспокоить
      
    ChannelType:
      type: integer
      enum:
        - 0
        - 1
      x-enum-descriptions:
        - Текстовый
        - Голосовой


parameters:
  zavodchat_token:
    name: zavodchat_token
    in: cookie
    required: true
    description: Токен авторизации
    schema:
      type: string
      example: 3c3beb77-b54a-4733-8c5d-acd7b2680241
